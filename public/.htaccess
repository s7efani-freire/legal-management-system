RewriteEngine On

# Se for arquivo/pasta real, serve normalmente
RewriteCond %{REQUEST_FILENAME} -f [OR]
RewriteCond %{REQUEST_FILENAME} -d
RewriteRule ^ - [L]

# Tudo que começar com /api/ vai para /api/index.php
RewriteRule ^api/ api/index.php [QSA,L]
